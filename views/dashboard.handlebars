<div class = "row">
    <div class= "col-12">
        <h2 class="text-center">Welcome to the Tech Blog</h2>
        <p class="text-center">The Tech Blog is a place where you can interact and discuss with others about technology. You can read posts from other users and create your very own!.</p>
    </div>
</div>
<div class="container blogBox">
    <div class="row header">
    <div class="col-8">
        <h2 class="text-center">Recent Posts</h2>
    </div>
</div>
<div class="col-md-6">
    <h3>Create a New Blog:</h3>

    <form class="form new-blog-form">
      <div class="form-group">
        <label for="blog-name">blog title:</label>
        <input class="form-input" type="text" id="blog-name" name="blog-name" />
      </div>
     
      <div class="form-group">
        <label for="blog-desc">description:</label>
        <textarea class="form-input" id="blog-desc" name="blog-desc"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>
<div class="row">
    <div class="col-8">
        {{#each blogs}}
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{title}}</h5>
                    <p class="card-text">{{content}}</p>
                    <p class="card-text">{{format_date date_created}}</p>
                    <p class="card-text">{{user.username}}</p>
                  
                     <button class="btn btn-primary" id="edit">Edit</button>
                <button class="btn btn-danger" id="delete">Delete</button>
                </div>
            </div>
        {{/each}}
    </div>
</div>

<script src="/js/dashboard.js"></script>

    
                    